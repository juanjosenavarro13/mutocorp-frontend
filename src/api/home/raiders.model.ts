export interface Raider {
  _id: string;
  name: string;
  hikoins: string;
  user: string;
  createdAt: string;
  updatedAt: string;
}

export interface TableRaiders {
  Name: string;
  Hikoins: string;
}
